# Translation Fidelity Matrix

> GENERATED by `tests/fidelity_matrix.rs`. Do not edit by hand — run
> `UPDATE_FIDELITY=1 cargo test -p gateway-llm --test fidelity_matrix`.

Per-feature support by egress provider family. `native` = first-class;
`emulated` = preserved via a translation shim; `warn` = dropped with a
surfaced warning; `unsupported` = request rejected (no silent degradation).

| Feature | OpenAI | Anthropic | Gemini |
|---|---|---|---|
| `response_format: json_schema` | native | emulated (forced tool) | native |
| tool/function calling | native | native | native |
| parallel tool calls | native | native | native |
| streaming tool-call deltas | native (aggregated) | native (aggregated) | native (aggregated) |
| vision (image parts) | native | native | native |
| `reasoning_effort` | native | warn (not mapped yet) | warn (not mapped yet) |
| prompt cache accounting | native | native | native |
| `logit_bias` / `seed` / `n` | warn (dropped) | warn (dropped) | warn (dropped) |
| audio input | unsupported | unsupported | unsupported |
